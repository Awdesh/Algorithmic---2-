using System;

namespace MSSelection
{
	public class SolveStringArithmetic
	{
		public SolveStringArithmetic ()
		{
		}

		/// <summary>
		/// Solve the specified input.
		/// </summary>
		/// <param name="input">Input.</param>
		// public int Solve(string input)
		// {
		// 	int length = input.Length;
		// 	int num = 0;
		// 	int i = 0;
		// 	bool isNeg = false;

		// 	if (input [i] == '-') {
		// 		i++;
		// 		isNeg = true;
		// 	}

		// 	while (i<length) {
		// 		num = num * 10; // while thinking consider this as step-2.
		// 		num = num + (input [i] - '0'); // while thinking consider this as step- 1.
		// 		i++;
		// 	}

		// 	if (isNeg) {
		// 		num *= -1;
		// 	}

		// 	return num;
		// }
	}
}

